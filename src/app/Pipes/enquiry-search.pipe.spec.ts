import { EnquirySearchPipe } from './enquiry-search.pipe';

describe('EnquirySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new EnquirySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
