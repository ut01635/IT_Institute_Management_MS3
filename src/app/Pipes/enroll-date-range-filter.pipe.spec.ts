import { EnrollDateRangeFilterPipe } from './enroll-date-range-filter.pipe';

describe('EnrollDateRangeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EnrollDateRangeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
