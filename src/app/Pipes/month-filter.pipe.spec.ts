import { MonthFilterPipe } from './month-filter.pipe';

describe('MonthFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
